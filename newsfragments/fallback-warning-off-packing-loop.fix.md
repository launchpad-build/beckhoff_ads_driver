The dropped-command warning moves out of the write() packing loop. Each affected interface evaluated a throttled warning per cycle inside the per-item loop; the loop now only notes the first affected interface and one throttled warning after the loop reports the count with that name as the example. Nothing about what is written to the PLC changes. (fallback-warning-off-packing-loop)
