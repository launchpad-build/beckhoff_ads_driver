The ADS request timeout now defaults to 100 ms instead of the library's 5000 ms. A blocking ADS call bounds how long stopping the I/O threads can stall on teardown, and how long a dead link can sit undetected inside one round-trip, so five seconds suited neither. The ads_request_timeout_ms hardware parameter overrides the value and 0 keeps the library default. (ads-request-timeout)
