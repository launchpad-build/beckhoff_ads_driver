name: Release version on merge to main

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  release:
    uses: launchpad-build/shared-workflows/.github/workflows/release-on-merge.yml@2.0.0
    with:
      version-source: package-xml
    secrets: inherit
